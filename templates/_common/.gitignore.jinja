# Minted from Mint's templates/_common/. One file for both languages, on
# purpose: a Go service that ignores __pycache__/ costs nothing, and two
# per-language copies of this file would be two things to keep in step.
#
# NOT ignored, deliberately: openapi.json and llms.txt. They are generated,
# and they are committed so that drift shows up as a PR diff.

# Local overrides — see docs/config.md
.env
config/config.local.yaml

# Build artifacts
/bin/
/dist/
*.test
coverage.out
.coverage
htmlcov/

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/
