<!--
  AGENTS.md is the source of truth for agent context in this service. This
  file exists so that tools looking for CLAUDE.md find the same content, and
  it imports rather than restates it — two copies would drift within a week.

  Mint's own repo makes this a symlink. It cannot ship as one: Copier's
  `preserve_symlinks` is off, which is what lets `docs/` land as real files
  rather than links back into the template, and it applies to every link.
  Replace this with `ln -sf AGENTS.md CLAUDE.md` if you prefer.

  Edit AGENTS.md, never this file.
-->

@AGENTS.md
