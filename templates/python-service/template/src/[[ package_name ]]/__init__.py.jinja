"""[[ service_name ]] — [[ service_description ]].

This package is the service. Everything under `internal/` is private to it:
nothing outside this distribution should import from there, and the layout
mirrors Go's `internal/` for exactly that reason.

The entry point is `__main__.py`, so the service starts with
`python -m [[ package_name ]]` — never the `uvicorn` CLI, which installs its
own logging before this package is imported. `make run` does it correctly.

Belongs here: nothing, for now. Keep this package free of code so that
`import [[ package_name ]]` stays free of side effects.
"""
