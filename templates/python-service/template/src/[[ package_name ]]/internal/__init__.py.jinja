"""Private implementation of this service.

Named `internal` after Go's compiler-enforced convention. Python does not
enforce it, so it is a rule instead: nothing outside this distribution imports
from here, and the two languages' trees stay directory-for-directory
identical.

Belongs here: nothing directly. Every child package below states what belongs
in it — read that before adding a file.
"""
